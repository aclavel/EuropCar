<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alexis.projeteuropcar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.ConnexionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.InscriptionActivity" />
        <activity android:name=".Activity.UpdateAgenceActivity" />
        <activity android:name=".Activity.AccueilActivity" />
        <activity android:name=".Activity.LocationListActivity" />
        <activity android:name=".Activity.VehiculeListActivity" />
        <activity android:name=".Activity.DetailLocationActivity"></activity>
    </application>

</manifest>